plugins:
  debug-container:
    shortCut: Shift-D
    description: Debug
    dangerous: true
    scopes:
      - containers
    command: kubectl
    background: false
    confirm: true
    args:
      - debug
      - -it
      - --context
      - $CONTEXT
      - -n
      - $NAMESPACE
      - $POD
      - --target
      - $NAME
      - --image=nicolaka/netshoot:latest
      - --profile=sysadmin
      - --share-processes
      - --
      - bash
  debug-node:
    shortCut: Shift-D
    description: Debug
    dangerous: true
    scopes:
      - nodes
    command: kubectl
    background: false
    confirm: true
    args:
      - debug
      - -it
      - --context
      - $CONTEXT
      - -n
      - kube-system
      - node/$NAME
      - --image=nicolaka/netshoot:latest
      - --profile=sysadmin
      - --
      - bash
  watch-events:
    shortCut: Shift-E
    confirm: false
    description: Watch Events
    scopes:
      - all
    command: kubectl
    background: false
    args:
      - events
      - --context
      - $CONTEXT
      - -n
      - $NAMESPACE
      - --for
      - $RESOURCE_NAME/$NAME
      - --watch
